
(define (add-terms))

(define (mul-terms))

(define (num-term-by-all-terms))

(define (sub-terms))

(define (add-poly))

(define (variable))

(define (term-list))

(define (minus-term))

(define (make-term))

(define (first-term))

(define (rest-terms))

(define (order))

(define (coeff))

;(define (adjoin-term term term-list))